const { validateSchedulePayload } = require('./server.js');
// Cannot easily import ES module from server.js which is esbuild format ESM
// Instead I will just write a simple verification node script
